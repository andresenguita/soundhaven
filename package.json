{
  "name": "soundhaven-monorepo",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm:server\" \"npm:client\"",
    "server": "npm --prefix server run dev",
    "client": "npm --prefix client run dev"
  },
  "devDependencies": { "concurrently": "^8.0.0" }
}
